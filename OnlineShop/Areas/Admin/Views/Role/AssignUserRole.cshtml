@{
    ViewData["Title"] = "AssignUserRole";
}
<br />
<div class="row">
    <div class="col-6">
        <h2 class="text-info">Assign User Role list</h2>
    </div>
    @*<div class="col-6 text-right">
        <a asp-action="Create" class="btn btn-info">&nbsp; <i class="fas fa-plus-circle"></i> New Role</a>
    </div>*@
</div>
<div>
    <table class="table table-striped broder">
        <tr class="table-info">
            <th>
               Full Name
            </th>
            <th>User Name</th>
            <th>Role Name</th>
        </tr>
        @foreach (var item in ViewBag.UserRoles)
        {
    <tr>
        <td>@item.FirstName @item.LastName </td>
        <td>@item.UserName</td>
        <td>@item.RoleName</td>
        <!--<td>
        <div class="btn-group">
            <a asp-action="Edit" class="btn btn-primary" asp-route-id=">
                <i class="fas fa-file-edit"></i>
            </a>-->
        @*<a asp-action="Details" class="btn btn-success" asp-route-id="@item.Id">
            <i class="far fa-list-alt"></i>
        </a>*@
        @*<a asp-action="Delete" asp-controller="SpecialTag" class="btn btn-danger" asp-route-id="@Model">*@
        <!--<a asp-action="Delete" class="btn btn-danger" asp-route-id="">

                <i class="fa fa-ban"></i>
            </a>
        </div>
    </td>-->
    </tr>
        }
    </table>
</div>
@section scripts{
    <script src="//cdn.jsdelivr.net/npm/alertifyjs@1.13.1/build/alertify.min.js"></script>
    <script type="text/javascript">
        $(function(){
            var save = '@TempData["Save"]'
            if(save!='') {
                alertify.success(save);
            }
            var update = '@TempData["update"]'
            if(update!='') {
                alertify.success(update);
            }
            var del = '@TempData["delete"]'
            if (del!='') {
                alertify.error(del);
            }
        })
    </script>
}